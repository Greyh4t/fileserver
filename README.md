# fileserver

simple file server, support manage files

## Usage
```
Usage of fileserver.exe:
  -dir string
        file dir (default "./")
  -pass string
        password
  -port string
        http port (default "80")
  -user string
        username
```

## View
![](view.png)
